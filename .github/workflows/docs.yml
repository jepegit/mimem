name: docs

on:
  push:
    branches: [main]
  workflow_dispatch:

# One deployment at a time; a queued run supersedes a running one rather than racing it.
concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

env:
  UV_PYTHON_DOWNLOADS: automatic

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: "3.13"

      - name: Install
        run: uv sync --all-extras --locked

      - name: Build
        run: uv run zensical build --clean

      # Pages has to be switched on by hand once, in Settings -> Pages -> Source: GitHub
      # Actions. The `enablement: true` option on this action exists for exactly this and was
      # tried first; the workflow's own token cannot create a Pages site ("Resource not
      # accessible by integration"), so the switch is a person's job. Until it is flipped this
      # job fails on the first step, which is the right kind of loud.
      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - uses: actions/deploy-pages@v4
        id: deployment
