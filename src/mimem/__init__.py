"""mimem -- convert your library into audio that is designed for remembering.

The design rules the generated documents must obey live in ``docs/DESIGN-RULES.md``; the
evidence behind them is in ``docs/knowledge-base/``. Code that implements a rule should name it
(``# SEG-01``) so the two stay connected.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
